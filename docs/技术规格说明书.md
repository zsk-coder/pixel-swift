# PixelSwift 技术方案文档

> **技术栈**: Vue 3 + Nuxt 4 + @nuxtjs/i18n + TailwindCSS + SCSS  
> **版本**: v1.0  
> **更新日期**: 2026-02-08  
> **关联文档**: [PRD.md](./PRD.md)

---

## 1. 技术架构总览

### 1.1 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                          客户端 (浏览器)                         │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────┐   │
│  │  Nuxt 4 SSR  │  │  Vue 3 SPA   │  │  Web Workers       │   │
│  │  (SEO页面)   │  │  (交互逻辑)  │  │  (图片处理线程)    │   │
│  └──────┬───────┘  └──────┬───────┘  └────────┬───────────┘   │
│         │                 │                    │               │
│  ┌──────┴─────────────────┴────────────────────┴───────────┐   │
│  │                     核心处理引擎                          │   │
│  │  Canvas API · OffscreenCanvas · WASM (HEIC解码)          │   │
│  └──────────────────────────────────────────────────────────┘   │
│         │                 │                    │               │
│  ┌──────┴───────┐  ┌─────┴──────┐  ┌─────────┴───────────┐   │
│  │  @nuxtjs/i18n│  │ TailwindCSS│  │  Google AdSense     │   │
│  │  (8种语言)   │  │  + SCSS    │  │  (广告集成)          │   │
│  └──────────────┘  └────────────┘  └─────────────────────┘   │
├─────────────────────────────────────────────────────────────────┤
│                    静态部署 (Vercel / Cloudflare Pages)          │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 核心设计原则

| 原则                | 说明                                   |
| ------------------- | -------------------------------------- |
| **零服务器处理**    | 所有图片处理在浏览器本地完成，无需后端 |
| **SSR + CSR 混合**  | SEO 着陆页 SSR 渲染，功能交互 CSR 接管 |
| **Web Worker 隔离** | 图片处理在 Worker 线程执行，不阻塞 UI  |
| **渐进增强**        | 基础功能不依赖高级 API，有则增强体验   |

---

## 2. 项目目录结构

```
pixelswift/
├── nuxt.config.ts              # Nuxt 配置入口
├── tailwind.config.ts          # TailwindCSS 配置
├── i18n.config.ts              # i18n 配置
├── app.vue                     # 根组件
│
├── assets/
│   ├── scss/
│   │   ├── _variables.scss     # SCSS 变量 (设计 Token)
│   │   ├── _mixins.scss        # 通用 mixin
│   │   ├── _animations.scss    # 动效定义
│   │   └── main.scss           # 入口样式
│   └── images/                 # 静态图片资源
│
├── components/
│   ├── common/                 # 通用组件
│   │   ├── AppHeader.vue       # 导航栏 (含汉堡菜单)
│   │   ├── AppFooter.vue       # 页脚
│   │   ├── FileUploader.vue    # 拖拽上传组件
│   │   ├── FileList.vue        # 文件列表组件
│   │   ├── DownloadButton.vue  # 下载按钮组件
│   │   ├── QualitySlider.vue   # 质量滑块组件
│   │   ├── ImagePreview.vue    # 图片预览组件
│   │   ├── CompareSlider.vue   # 滑动对比组件
│   │   ├── ProgressBar.vue     # 进度条组件
│   │   ├── ErrorToast.vue      # 错误提示组件
│   │   └── AdSlot.vue          # 广告位组件
│   │
│   ├── converter/              # 转换器专属组件
│   │   ├── FormatSelector.vue  # 格式选择器
│   │   └── PdfOptions.vue      # PDF特殊选项
│   │
│   ├── compressor/             # 压缩器专属组件
│   │   ├── PresetButtons.vue   # 压缩档位预设
│   │   └── DualPreview.vue     # 双栏对比预览
│   │
│   └── resizer/                # 调整器专属组件
│       ├── DimensionInput.vue  # 尺寸输入组件
│       ├── RatioSelector.vue   # 宽高比选择器
│       └── ModeSwitch.vue      # 模式切换组件
│
├── composables/                # 组合式函数
│   ├── useImageProcessor.ts    # 图片处理核心逻辑
│   ├── useFileUpload.ts        # 文件上传逻辑
│   ├── useDownload.ts          # 文件下载逻辑 (含ZIP)
│   ├── useBatchProcess.ts      # 批量处理逻辑
│   ├── useImageCompare.ts      # 图片对比逻辑
│   └── useTheme.ts             # 主题切换逻辑
│
├── workers/                    # Web Worker
│   ├── imageProcessor.worker.ts  # 图片处理 Worker
│   └── zipPacker.worker.ts       # ZIP 打包 Worker
│
├── utils/
│   ├── imageCodec.ts           # 编解码工具 (含 HEIC)
│   ├── fileNaming.ts           # 文件命名规则
│   ├── formatDetector.ts       # 格式检测
│   └── constants.ts            # 全局常量
│
├── pages/
│   ├── index.vue               # 首页
│   ├── converter.vue           # 通用转换器页
│   ├── compress-image.vue      # 图片压缩页
│   ├── resize-image.vue        # 尺寸调整页
│   ├── [from]-to-[to].vue      # 动态 SEO 着陆页 (如 heic-to-jpg)
│   └── [...slug].vue           # 404 兜底
│
├── layouts/
│   ├── default.vue             # 默认布局 (Header + Footer + Ad)
│   └── minimal.vue             # 简洁布局 (无广告)
│
├── plugins/
│   ├── analytics.client.ts     # GA4 / 事件埋点
│   └── adsense.client.ts       # Google AdSense
│
├── server/
│   └── routes/
│       └── sitemap.xml.ts      # 动态 sitemap 生成
│
├── locales/                    # 多语言文件
│   ├── en.json
│   ├── zh.json
│   ├── es.json
│   ├── ja.json
│   ├── de.json
│   ├── fr.json
│   ├── pt.json
│   └── ko.json
│
└── public/
    ├── favicon.ico
    ├── robots.txt
    └── og-image.png            # 社交分享图
```

---

## 3. 核心模块技术方案

### 3.1 图片处理引擎

所有图片处理在浏览器端完成，通过 Web Worker 避免阻塞主线程。

#### 3.1.1 处理流程

```
用户上传文件
    │
    ▼
主线程: File → ArrayBuffer → 传递给 Worker
    │
    ▼
Worker线程:
    ├── 1. 格式检测 (magic bytes)
    ├── 2. 解码 (Canvas/WASM)
    ├── 3. 处理 (转换/压缩/缩放)
    ├── 4. 编码 (canvas.toBlob)
    └── 5. 返回 Blob + 元数据
    │
    ▼
主线程: 更新UI状态 → 生成下载链接
```

#### 3.1.2 composable 核心接口

```typescript
// composables/useImageProcessor.ts

interface ProcessOptions {
  action: "convert" | "compress" | "resize";
  outputFormat?: "jpg" | "png" | "webp" | "pdf";
  quality?: number; // 1-100
  width?: number;
  height?: number;
  keepAspectRatio?: boolean;
  progressive?: boolean; // 渐进式 JPEG
}

interface ProcessResult {
  blob: Blob;
  originalSize: number;
  processedSize: number;
  width: number;
  height: number;
  format: string;
  duration: number; // 处理耗时 ms
}

export function useImageProcessor() {
  const isProcessing = ref(false);
  const progress = ref(0);
  const error = ref<string | null>(null);

  async function processImage(
    file: File,
    options: ProcessOptions,
  ): Promise<ProcessResult> {
    /* ... */
  }

  async function processBatch(
    files: File[],
    options: ProcessOptions,
    onProgress: (index: number, result: ProcessResult) => void,
  ): Promise<ProcessResult[]> {
    /* ... */
  }

  return { isProcessing, progress, error, processImage, processBatch };
}
```

#### 3.1.3 Web Worker 通信协议

```typescript
// Worker 接收消息
interface WorkerInput {
  id: string; // 任务唯一ID
  action: ProcessOptions["action"];
  buffer: ArrayBuffer; // 图片二进制数据
  options: ProcessOptions;
}

// Worker 发送消息
interface WorkerOutput {
  id: string;
  type: "progress" | "complete" | "error";
  progress?: number; // 0-100
  result?: {
    buffer: ArrayBuffer;
    metadata: Omit<ProcessResult, "blob">;
  };
  error?: string;
}
```

#### 3.1.4 格式处理策略

| 输入格式         | 解码方式                                     | 浏览器兼容性              | 降级方案               |
| ---------------- | -------------------------------------------- | ------------------------- | ---------------------- |
| JPG/PNG/WebP/BMP | `createImageBitmap()` / Canvas `drawImage()` | 全部浏览器                | 无需降级               |
| HEIC/HEIF        | `heic2any` (WASM)                            | 需加载 WASM 模块 (~200KB) | 提示用户手动转换后上传 |
| TIFF             | `UTIF.js`                                    | 需第三方库                | 提示格式不支持         |

| 输出格式 | 编码方式                               | 关键参数             |
| -------- | -------------------------------------- | -------------------- |
| JPG      | `canvas.toBlob('image/jpeg', quality)` | quality: 0-1         |
| PNG      | `canvas.toBlob('image/png')`           | 无质量参数，始终无损 |
| WebP     | `canvas.toBlob('image/webp', quality)` | quality: 0-1         |
| PDF      | `jsPDF` + Canvas 图片嵌入              | 页面尺寸/方向        |

> [!IMPORTANT]
> **HEIC 解码限制**: Safari 原生支持 HEIC，Chrome/Firefox 需要通过 `heic2any` WASM 库解码。该库约 200KB gzip，应**懒加载**，仅在用户上传 HEIC 文件时动态 import。

### 3.2 文件上传模块

```typescript
// composables/useFileUpload.ts

interface UploadConfig {
  maxFileSize: number; // 默认 50MB
  maxFileCount: number; // 默认 20
  acceptFormats: string[]; // MIME types
}

interface UploadFile {
  id: string;
  file: File;
  name: string;
  size: number;
  format: string;
  preview: string; // Object URL
  status: "pending" | "processing" | "done" | "error";
  progress: number;
  result?: ProcessResult;
  error?: string;
}

export function useFileUpload(config: UploadConfig) {
  const files = ref<UploadFile[]>([]);

  // 拖拽上传
  function handleDrop(e: DragEvent): void {
    /* ... */
  }

  // 点击上传
  function handleFileSelect(e: Event): void {
    /* ... */
  }

  // 粘贴上传 (Ctrl+V)
  function handlePaste(e: ClipboardEvent): void {
    /* ... */
  }

  // 文件校验
  function validateFile(file: File): { valid: boolean; error?: string } {
    /* ... */
  }

  // 移除文件
  function removeFile(id: string): void {
    /* ... */
  }

  // 清空所有
  function clearAll(): void {
    /* ... */
  }

  return {
    files,
    handleDrop,
    handleFileSelect,
    handlePaste,
    removeFile,
    clearAll,
  };
}
```

### 3.3 下载模块

```typescript
// composables/useDownload.ts

export function useDownload() {
  // 单文件下载
  function downloadFile(blob: Blob, filename: string): void {
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = filename;
    a.click();
    URL.revokeObjectURL(url);
  }

  // 批量ZIP打包下载 (在 Worker 中执行)
  async function downloadAsZip(
    files: Array<{ blob: Blob; name: string }>,
    zipName: string,
  ): Promise<void> {
    /* 使用 JSZip 在 Worker 中打包 */
  }

  // 文件命名
  function generateFileName(
    original: string,
    action: "convert" | "compress" | "resize",
    meta?: { format?: string; width?: number; height?: number },
  ): string {
    /* PRD 3.2.2 命名规则 */
  }

  return { downloadFile, downloadAsZip, generateFileName };
}
```

---

## 4. 国际化 (i18n) 方案

### 4.1 Nuxt i18n 配置

```typescript
// i18n.config.ts
export default defineI18nConfig(() => ({
  legacy: false,
  locale: "en",
  fallbackLocale: "en",
  messages: {}, // 懒加载，不在此处引入
}));
```

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  modules: ["@nuxtjs/i18n"],

  i18n: {
    locales: [
      { code: "en", language: "en-US", file: "en.json", name: "English" },
      { code: "zh", language: "zh-CN", file: "zh.json", name: "简体中文" },
      { code: "es", language: "es-ES", file: "es.json", name: "Español" },
      { code: "ja", language: "ja-JP", file: "ja.json", name: "日本語" },
      { code: "de", language: "de-DE", file: "de.json", name: "Deutsch" },
      { code: "fr", language: "fr-FR", file: "fr.json", name: "Français" },
      { code: "pt", language: "pt-BR", file: "pt.json", name: "Português" },
      { code: "ko", language: "ko-KR", file: "ko.json", name: "한국어" },
    ],
    lazy: true,
    langDir: "locales/",
    defaultLocale: "en",
    strategy: "prefix_except_default", // /en/xxx → /xxx,  /zh/xxx 保留前缀
    detectBrowserLanguage: {
      useCookie: true,
      cookieKey: "i18n_lang",
      redirectOn: "root", // 仅首页自动跳转
    },
  },
});
```

### 4.2 SEO 着陆页路由

通过动态路由 `[from]-to-[to].vue` 生成格式转换的 SEO 页面：

```typescript
// pages/[from]-to-[to].vue
const route = useRoute();
const { from, to } = route.params; // 如: heic, jpg

// 校验合法的格式组合
const VALID_CONVERSIONS = {
  "heic-to-jpg": { input: "heic", output: "jpg" },
  "png-to-webp": { input: "png", output: "webp" },
  "jpg-to-pdf": { input: "jpg", output: "pdf" },
  "webp-to-png": { input: "webp", output: "png" },
  "bmp-to-jpg": { input: "bmp", output: "jpg" },
  // ... 更多组合
};
```

### 4.3 SEO Meta 自动生成

```typescript
// composables/useSeoMeta.ts
export function usePageSeo(pageKey: string) {
  const { t, locale } = useI18n();
  const localePath = useLocalePath();

  useHead({
    title: t(`seo.${pageKey}.title`),
    meta: [
      { name: "description", content: t(`seo.${pageKey}.description`) },
      { property: "og:title", content: t(`seo.${pageKey}.title`) },
      { property: "og:description", content: t(`seo.${pageKey}.description`) },
    ],
    link: [
      // hreflang 标签自动生成
      ...useI18nHead({ addSeoAttributes: true }).value.link,
    ],
  });

  // 结构化数据 (FAQ Schema)
  useSchemaOrg([
    defineWebPage({ name: t(`seo.${pageKey}.title`) }),
    defineFAQPage({
      mainEntity: t(`seo.${pageKey}.faq`),
    }),
  ]);
}
```

### 4.4 多语言文件结构

```jsonc
// locales/en.json
{
  "nav": {
    "converter": "Converter",
    "compressor": "Compressor",
    "resizer": "Resize"
  },
  "upload": {
    "dragText": "Drag & drop images here, or click to upload",
    "supportFormats": "Supports: JPG, PNG, WebP, HEIC, BMP, TIFF",
    "maxFiles": "Up to {count} images, max {size}MB each",
    "errors": {
      "formatNotSupported": "Unsupported format. Please upload {formats}",
      "fileTooLarge": "File exceeds {size}MB limit",
      "fileCorrupted": "File appears to be corrupted",
      "processFailed": "Processing failed. Please refresh and try again"
    }
  },
  "converter": {
    "title": "Image Format Converter",
    "outputFormat": "Output Format",
    "quality": "Quality",
    "progressive": "Progressive JPEG (recommended)"
  },
  "compressor": {
    "title": "Image Compressor",
    "presets": {
      "extreme": "Maximum",
      "recommended": "Recommended",
      "light": "Light",
      "lossless": "Near Lossless"
    },
    "savedSpace": "Saved: {size} ({percent}%)"
  },
  "seo": {
    "heic-to-jpg": {
      "title": "Free HEIC to JPG Converter Online",
      "description": "Convert HEIC to JPG online for free. No upload needed - process images in your browser. Fast, secure, unlimited.",
      "faq": [...]
    }
  }
}
```

---

## 5. 样式架构

### 5.1 TailwindCSS 配置

```typescript
// tailwind.config.ts
import type { Config } from "tailwindcss";

export default {
  content: ["./components/**/*.vue", "./layouts/**/*.vue", "./pages/**/*.vue"],
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        brand: { DEFAULT: "#2563EB", dark: "#1D4ED8", light: "#3B82F6" },
        success: "#10B981",
        warning: "#F59E0B",
        danger: "#EF4444",
        text: { primary: "#1F2937", secondary: "#6B7280" },
        surface: { DEFAULT: "#F9FAFB", card: "#FFFFFF", border: "#E5E7EB" },
        dark: {
          bg: "#111827",
          card: "#1F2937",
          text: "#F9FAFB",
          border: "#374151",
        },
      },
      fontFamily: {
        sans: ["Inter", "Noto Sans SC", "system-ui", "sans-serif"],
      },
      fontSize: {
        "page-title": ["32px", { lineHeight: "1.2", fontWeight: "700" }],
        section: ["24px", { lineHeight: "1.3", fontWeight: "600" }],
        body: ["16px", { lineHeight: "1.6", fontWeight: "400" }],
        button: ["16px", { lineHeight: "1", fontWeight: "500" }],
        caption: ["14px", { lineHeight: "1.5", fontWeight: "400" }],
      },
      spacing: {
        xs: "4px",
        sm: "8px",
        md: "16px",
        lg: "24px",
        xl: "32px",
        "2xl": "48px",
      },
      borderRadius: {
        btn: "8px",
        card: "12px",
        input: "8px",
        modal: "16px",
      },
      boxShadow: {
        low: "0 1px 2px rgba(0,0,0,0.05)",
        mid: "0 4px 6px rgba(0,0,0,0.1)",
        high: "0 10px 15px rgba(0,0,0,0.1)",
      },
      maxWidth: {
        content: "1200px",
      },
    },
  },
} satisfies Config;
```

### 5.2 SCSS 变量与 Mixin

```scss
// assets/scss/_variables.scss
// TailwindCSS 处理大部分样式，SCSS 用于复杂逻辑和动效

// 响应式断点 (与 Tailwind 保持一致)
$breakpoints: (
  "xs": 576px,
  "sm": 768px,
  "md": 1024px,
  "lg": 1280px,
);

// 动效时长
$transition-fast: 150ms;
$transition-normal: 200ms;
$transition-slow: 300ms;
```

```scss
// assets/scss/_mixins.scss
@mixin respond-to($breakpoint) {
  @media (min-width: map-get($breakpoints, $breakpoint)) {
    @content;
  }
}

// 仅在复杂动效场景中使用
@mixin hover-lift {
  transition:
    box-shadow $transition-normal ease-out,
    transform $transition-normal ease-out;
  &:hover {
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transform: translateY(-2px);
  }
}
```

### 5.3 深色模式实现

```typescript
// composables/useTheme.ts
export function useTheme() {
  const colorMode = useColorMode();

  const isDark = computed(() => colorMode.value === "dark");

  function toggleTheme() {
    colorMode.preference = isDark.value ? "light" : "dark";
  }

  return { isDark, toggleTheme };
}
```

TailwindCSS 中通过 `dark:` 前缀实现：

```html
<div class="bg-surface dark:bg-dark-bg text-text-primary dark:text-dark-text">
  <div
    class="bg-surface-card dark:bg-dark-card border border-surface-border dark:border-dark-border rounded-card shadow-low"
  >
    <!-- content -->
  </div>
</div>
```

---

## 6. 关键组件技术设计

### 6.1 FileUploader 组件

```vue
<!-- components/common/FileUploader.vue -->
<template>
  <div
    class="relative border-2 border-dashed rounded-card p-2xl text-center
           transition-all duration-200 cursor-pointer
           border-surface-border hover:border-brand
           dark:border-dark-border dark:hover:border-brand"
    :class="{
      'border-brand bg-brand/5 border-solid': isDragOver,
      'opacity-50 pointer-events-none': isProcessing,
    }"
    @dragover.prevent="onDragOver"
    @dragleave="onDragLeave"
    @drop.prevent="onDrop"
    @click="triggerFileInput"
  >
    <input
      ref="fileInput"
      type="file"
      :accept="acceptString"
      multiple
      class="hidden"
      @change="onFileSelect"
    />

    <div v-if="!isDragOver">
      <p class="text-section mb-md">🖼️ {{ $t("upload.dragText") }}</p>
      <p class="text-caption text-text-secondary">
        {{ $t("upload.supportFormats") }}
      </p>
      <p class="text-caption text-text-secondary">
        {{ $t("upload.maxFiles", { count: maxCount, size: maxSizeMB }) }}
      </p>
    </div>

    <div v-else class="text-brand text-section">
      {{ $t("upload.releaseToUpload") }}
    </div>
  </div>
</template>
```

### 6.2 CompareSlider 滑动对比组件

```vue
<!-- components/common/CompareSlider.vue -->
<script setup lang="ts">
const props = defineProps<{
  originalSrc: string;
  compressedSrc: string;
}>();

const containerRef = ref<HTMLDivElement>();
const sliderPosition = ref(50); // 百分比
const isDragging = ref(false);

function onPointerMove(e: PointerEvent) {
  if (!isDragging.value || !containerRef.value) return;
  const rect = containerRef.value.getBoundingClientRect();
  const x = Math.max(0, Math.min(e.clientX - rect.left, rect.width));
  sliderPosition.value = (x / rect.width) * 100;
}
</script>

<template>
  <div
    ref="containerRef"
    class="relative overflow-hidden rounded-card select-none cursor-col-resize"
    @pointerdown="isDragging = true"
    @pointermove="onPointerMove"
    @pointerup="isDragging = false"
  >
    <!-- 原图 (全幅) -->
    <img :src="originalSrc" class="w-full block" alt="original" />

    <!-- 压缩后 (clip 裁剪) -->
    <img
      :src="compressedSrc"
      class="absolute inset-0 w-full block"
      :style="{ clipPath: `inset(0 0 0 ${sliderPosition}%)` }"
      alt="compressed"
    />

    <!-- 分隔线 -->
    <div
      class="absolute top-0 bottom-0 w-0.5 bg-white shadow-high z-10"
      :style="{ left: `${sliderPosition}%` }"
    >
      <div
        class="absolute top-1/2 -translate-y-1/2 -translate-x-1/2
                  w-8 h-8 rounded-full bg-white shadow-mid
                  flex items-center justify-center"
      >
        ↔
      </div>
    </div>
  </div>
</template>
```

### 6.3 QualitySlider 质量滑块

```vue
<!-- components/common/QualitySlider.vue -->
<script setup lang="ts">
const props = defineProps<{
  modelValue: number;
  presets?: Array<{ label: string; value: number }>;
}>();

const emit = defineEmits<{
  "update:modelValue": [value: number];
  change: [value: number]; // 释放时触发实际处理
}>();

const isDragging = ref(false);
let debounceTimer: ReturnType<typeof setTimeout>;

function onInput(value: number) {
  emit("update:modelValue", value);
  // 拖动中只更新 UI，不触发处理
}

function onRelease(value: number) {
  isDragging.value = false;
  // 释放后延迟 300ms 触发实际压缩 (PRD 2.2.5)
  clearTimeout(debounceTimer);
  debounceTimer = setTimeout(() => emit("change", value), 300);
}

function selectPreset(value: number) {
  emit("update:modelValue", value);
  emit("change", value); // 预设立即触发 (PRD 2.2.5)
}
</script>
```

---

## 7. 性能优化方案

### 7.1 核心 Web Vitals 目标

| 指标        | 目标                 | 实现手段                         |
| ----------- | -------------------- | -------------------------------- |
| LCP < 2.5s  | 首屏关键内容快速呈现 | SSR + 关键 CSS 内联 + 字体预加载 |
| FID < 100ms | 主线程不被阻塞       | 图片处理在 Web Worker 中执行     |
| CLS < 0.1   | 无布局偏移           | 广告位骨架屏占位 + 图片宽高预设  |

### 7.2 资源加载策略

| 资源                   | 加载策略              | 触发条件                           |
| ---------------------- | --------------------- | ---------------------------------- |
| 核心框架 (Vue/Nuxt)    | 首屏同步加载          | -                                  |
| TailwindCSS            | 编译时 purge 未用样式 | -                                  |
| Web Worker             | 懒加载                | 用户首次上传文件                   |
| heic2any WASM (~200KB) | 动态 import           | 检测到 HEIC 文件                   |
| jsPDF (~300KB)         | 动态 import           | 用户选择 PDF 输出                  |
| JSZip (~100KB)         | 动态 import           | 用户触发批量下载                   |
| Google AdSense         | 延迟加载              | 页面空闲时 (`requestIdleCallback`) |
| 非首屏语言包           | 按需加载              | 用户切换语言                       |

```typescript
// 懒加载示例: HEIC 解码
async function decodeHeic(buffer: ArrayBuffer) {
  const { default: heic2any } = await import("heic2any");
  return heic2any({ blob: new Blob([buffer]), toType: "image/jpeg" });
}
```

### 7.3 构建优化

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  // ...
  vite: {
    build: {
      rollupOptions: {
        output: {
          manualChunks: {
            "image-core": ["./composables/useImageProcessor"],
            "pdf-tools": ["jspdf"],
            "zip-tools": ["jszip"],
          },
        },
      },
    },
  },

  nitro: {
    prerender: {
      // 预渲染 SEO 着陆页
      routes: [
        "/heic-to-jpg",
        "/png-to-webp",
        "/jpg-to-pdf",
        "/compress-image",
        "/resize-image",
        // 各语言版本
        "/zh/heic-to-jpg",
        "/zh/png-to-webp",
        "/es/heic-to-jpg",
        "/ja/heic-to-jpg",
        // ...
      ],
    },
  },

  routeRules: {
    // 静态页面规则
    "/": { prerender: true },
    "/**": { swr: 3600 }, // ISR: 1小时缓存
  },
});
```

---

## 8. 广告集成方案

### 8.1 AdSlot 组件设计

```vue
<!-- components/common/AdSlot.vue -->
<script setup lang="ts">
const props = defineProps<{
  slot: "top-banner" | "sidebar" | "bottom-banner" | "interstitial";
  responsive?: boolean;
}>();

const adRef = ref<HTMLDivElement>();
const isLoaded = ref(false);
const isFailed = ref(false);

// 广告尺寸映射
const sizeMap = {
  "top-banner": { desktop: "728x90", mobile: "320x50" },
  sidebar: { desktop: "300x250", mobile: null }, // 移动端隐藏
  "bottom-banner": { desktop: "728x90", mobile: "320x100" },
  interstitial: { desktop: "300x250", mobile: "300x250" },
};

// IntersectionObserver 延迟加载
onMounted(() => {
  const observer = new IntersectionObserver(
    ([entry]) => {
      if (entry.isIntersecting) {
        loadAd();
        observer.disconnect();
      }
    },
    { rootMargin: "200px" },
  );

  if (adRef.value) observer.observe(adRef.value);
});
</script>

<template>
  <div
    v-if="sizeMap[slot].mobile !== null || !isMobile"
    ref="adRef"
    class="ad-slot flex items-center justify-center bg-surface-card rounded-card"
    :class="{ 'animate-pulse': !isLoaded && !isFailed }"
  >
    <!-- 骨架屏占位 -->
    <div v-if="!isLoaded && !isFailed" class="text-caption text-text-secondary">
      Ad
    </div>
    <!-- 广告加载失败则隐藏 -->
  </div>
</template>
```

### 8.2 插屏广告频次控制

```typescript
// utils/adFrequency.ts
const STORAGE_KEY = "ps_interstitial_count";
const MAX_PER_DAY = 3;

export function canShowInterstitial(): boolean {
  const data = JSON.parse(localStorage.getItem(STORAGE_KEY) || "{}");
  const today = new Date().toISOString().split("T")[0];

  if (data.date !== today) {
    return true; // 新的一天，重置计数
  }

  return (data.count || 0) < MAX_PER_DAY;
}

export function recordInterstitial(): void {
  const today = new Date().toISOString().split("T")[0];
  const data = JSON.parse(localStorage.getItem(STORAGE_KEY) || "{}");

  if (data.date !== today) {
    localStorage.setItem(
      STORAGE_KEY,
      JSON.stringify({ date: today, count: 1 }),
    );
  } else {
    data.count = (data.count || 0) + 1;
    localStorage.setItem(STORAGE_KEY, JSON.stringify(data));
  }
}
```

---

## 9. 无障碍 (Accessibility) 规范

| 要求       | 实现方式                                                                                |
| ---------- | --------------------------------------------------------------------------------------- |
| 键盘导航   | 所有交互元素支持 Tab 聚焦，按钮支持 Enter/Space                                         |
| 屏幕阅读器 | 上传区域 `role="button"` + `aria-label`，滑块 `role="slider"` + `aria-valuemin/max/now` |
| 颜色对比度 | 文本与背景对比度 ≥ 4.5:1 (WCAG AA)                                                      |
| 焦点指示器 | 可见的 focus ring (`focus-visible:ring-2 ring-brand`)                                   |
| Alt 文本   | 所有图片预览提供动态 alt 描述                                                           |
| 减少动画   | 尊重 `prefers-reduced-motion` 媒体查询                                                  |

```scss
// assets/scss/_animations.scss
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

## 10. 数据分析与埋点

### 10.1 关键事件定义

| 事件名             | 触发时机     | 参数                                          |
| ------------------ | ------------ | --------------------------------------------- |
| `file_upload`      | 文件上传成功 | `{ format, size, count }`                     |
| `process_start`    | 开始处理     | `{ action, format_in, format_out, quality }`  |
| `process_complete` | 处理完成     | `{ action, duration_ms, size_reduction_pct }` |
| `process_error`    | 处理失败     | `{ action, error_type, format }`              |
| `file_download`    | 文件下载     | `{ action, format, is_batch }`                |
| `lang_switch`      | 切换语言     | `{ from, to }`                                |
| `theme_switch`     | 切换主题     | `{ theme }`                                   |
| `ad_impression`    | 广告展示     | `{ slot, page }`                              |
| `ad_click`         | 广告点击     | `{ slot, page }`                              |

### 10.2 埋点实现

```typescript
// plugins/analytics.client.ts
export default defineNuxtPlugin(() => {
  // GA4 初始化
  if (import.meta.env.PROD) {
    useHead({
      script: [
        {
          src: `https://www.googletagmanager.com/gtag/js?id=${GA_ID}`,
          async: true,
        },
      ],
    });
  }

  return {
    provide: {
      track: (event: string, params?: Record<string, any>) => {
        window.gtag?.("event", event, params);
      },
    },
  };
});
```

---

## 11. 浏览器兼容性

| 浏览器  | 最低版本 | 关键依赖                                          |
| ------- | -------- | ------------------------------------------------- |
| Chrome  | 90+      | `createImageBitmap`, `OffscreenCanvas`, WebP 编码 |
| Firefox | 90+      | `createImageBitmap`, WebP 编码                    |
| Safari  | 15+      | WebP 编码 (Safari 16+), HEIC 原生解码             |
| Edge    | 90+      | 同 Chromium                                       |

| API                   | Chrome | Firefox | Safari   | 降级方案                       |
| --------------------- | ------ | ------- | -------- | ------------------------------ |
| `OffscreenCanvas`     | ✅     | ✅      | ✅ 16.4+ | 回退 `<canvas>` 主线程处理     |
| `createImageBitmap`   | ✅     | ✅      | ✅ 15+   | 回退 `Image()` + `drawImage()` |
| WebP encode           | ✅     | ✅      | ✅ 16+   | 回退 PNG 输出                  |
| Web Workers           | ✅     | ✅      | ✅       | 回退主线程 (性能降低)          |
| `ClipboardAPI` (粘贴) | ✅     | ✅      | ✅ 13.1+ | 隐藏粘贴上传按钮               |

---

## 12. 部署方案

### 12.1 推荐平台

| 平台                 | 优势                      | 适用场景 |
| -------------------- | ------------------------- | -------- |
| **Vercel**           | Nuxt 官方推荐，零配置部署 | 首选     |
| **Cloudflare Pages** | 全球边缘节点，速度极快    | 备选     |
| **Netlify**          | 简单易用                  | 备选     |

### 12.2 CI/CD 流程

```
main 分支 push
    │
    ▼
GitHub Actions
    ├── lint + type-check
    ├── nuxt build
    ├── Lighthouse CI (性能回归检测)
    └── 自动部署到 Vercel
```

### 12.3 环境变量

| 变量                     | 用途                  | 示例                     |
| ------------------------ | --------------------- | ------------------------ |
| `NUXT_PUBLIC_GA_ID`      | Google Analytics      | `G-XXXXXXXXXX`           |
| `NUXT_PUBLIC_ADSENSE_ID` | Google AdSense        | `ca-pub-XXXXXXX`         |
| `NUXT_PUBLIC_SITE_URL`   | 站点 URL (sitemap 用) | `https://pixelswift.com` |

---

## 13. 版本规划与里程碑

| 版本         | 功能范围                                | 验收标准                                        | 预估周期 |
| ------------ | --------------------------------------- | ----------------------------------------------- | -------- |
| **v1.0 MVP** | 转换器 + 压缩器 + 调整器 + 8语言 + 广告 | Lighthouse ≥ 90, 核心流程跑通, SEO 页全部可访问 | 4周      |
| **v1.1**     | 图片裁剪, 旋转/翻转                     | 新功能 E2E 通过                                 | +2周     |
| **v1.2**     | 水印添加/移除                           | 水印功能完整                                    | +4周     |
| **v2.0**     | 批量模板, API 开放                      | API 文档 + Rate Limit                           | +8周     |

---

## 14. 第三方依赖清单

| 包名              | 用途                  | 大小 (gzip) | 加载方式    |
| ----------------- | --------------------- | ----------- | ----------- |
| `heic2any`        | HEIC 解码             | ~200KB      | 动态 import |
| `jspdf`           | PDF 生成              | ~300KB      | 动态 import |
| `jszip`           | ZIP 打包              | ~100KB      | 动态 import |
| `utif`            | TIFF 解码             | ~50KB       | 动态 import |
| `@nuxt/image`     | 图片优化 (预览缩略图) | 内置        | -           |
| `nuxt-schema-org` | 结构化数据            | ~15KB       | 内置        |

> [!NOTE]
> 所有大型第三方库均采用**动态 import** 策略，不会影响首屏加载性能。核心处理逻辑使用原生 Canvas API，零外部依赖。

---

## 文档变更记录

| 版本 | 日期       | 变更内容 | 作者      |
| ---- | ---------- | -------- | --------- |
| v1.0 | 2026-02-08 | 初始版本 | Tech Team |
